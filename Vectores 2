public static void main(String[] args) {
		int[] vector = {1,6,3,2,5};
		int Numeroabuscar = 2;
		//[] la vaina sirve pa crear arrays (Acumulaciones de datos (int) o lo que quieras como booleans)
		for (int i = 0;i<vector.length;i++) {
			// for = recibe 3 valores, primero la variable (i) que la creas, segundo valor la condicion y el tercero lo que hace la i cada vez que recorre el bucle
			System.out.println(vector[i]);
			if (vector[i] == 6) {
				System.out.println("numero encontrado en la posicion "+ i);
				break;
				//el break pa romper el bucle
			}
		}
	
		
	}
	
}
	
